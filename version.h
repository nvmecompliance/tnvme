// Specify the software release version numbers on thier own line for use with
// awk and the creation of RPM's while also being compatible with building
// the binaries via the Makefile with *.cpp source code.
#define VER_MAJOR	\
0

#define VER_MINOR	\
1
